<h2> "<%= @track.song %>"</h2>

<h3> Album: <%= link_to "#{@track.album.title}", album_url(@track.album) %></h3>

<h3> Band/Artist: <%= link_to "#{@track.album.band.name}", band_url(@track.album.band) %></h3>

<h4>Modify Entry</h4>

<%= button_to "Edit", edit_track_url(@track), method: :get %> <br />
<%= button_to "Delete", track_url(@track), method: :delete %>
